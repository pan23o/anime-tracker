(()=>{'use strict';
const SUPABASE_URL='https://djfjqecahztogacliavh.supabase.co';
const SUPABASE_KEY='sb_publishable_tm8Tid_HSYtu6cxXQ3ddKA_RWN15BSB';
const KEY_STATE='anime_tracker_v6',KEY_PROFILE='anime_tracker_profile_v1',KEY_LAST='anime_tracker_last_save',KEY_ACH='anime_tracker_v6_unlocked_achievements',KEY_SETTINGS='anime_tracker_settings_v1';
const KEYS=[KEY_STATE,KEY_PROFILE,KEY_SETTINGS,'anime_tracker_activity',KEY_ACH,'anime_tracker_trash_v1','anime_tracker_backups_v1','anime_tracker_theme','anime_tracker_custom_theme',KEY_LAST];
const sb=window.supabase?.createClient(SUPABASE_URL,SUPABASE_KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});
if(!sb)return;
const $=id=>document.getElementById(id);
const read=(k,f)=>{try{const v=JSON.parse(localStorage.getItem(k)||'null');return v==null?f:v}catch{return f}};
const write=(k,v)=>{try{localStorage.setItem(k,JSON.stringify(v))}catch{}};
const notify=m=>window.toast?window.toast(m):console.info('[AnimeTracker]',m);
let user=null,modalBuilt=false,syncTimer=0;
function library(){const a=read(KEY_STATE,[]);return Array.isArray(a)?a.filter(x=>(x?.anime||'').trim()):[]}
function remoteLibrary(state){try{const v=state?.[KEY_STATE];const a=typeof v==='string'?JSON.parse(v||'[]'):v;return Array.isArray(a)?a.filter(x=>(x?.anime||'').trim()):[]}catch{return[]}}
function localStamp(){const raw=localStorage.getItem(KEY_LAST)||'';const n=Number(raw);if(Number.isFinite(n)&&n>0)return n;const t=Date.parse(raw);return Number.isFinite(t)?t:0}
function initials(){return(profile?.name||user?.email||'AT').trim().split(/\s+/).slice(0,2).map(x=>x[0]||'').join('').toUpperCase()||'AT'}
function setTopProfile(){document.querySelectorAll('.cloud-account-btn').forEach(x=>x.style.display='none');$('cloudUserMenu')?.classList.remove('show');const b=$('profileTopBtn');if(!b)return;b.innerHTML=profile?.avatar?`<img src="${profile.avatar}" alt="">`:`<span>${initials()}</span>`;b.title=user?(user.email||'Mi perfil'):'Mi perfil'}
function stats(){const a=library(),ach=read(KEY_ACH,[]);const vals=[a.length,a.reduce((n,x)=>n+(+x.watched||0),0),a.filter(x=>x.favorite).length,a.filter(x=>(+x.total||+x.knownTotal||0)>0&&(+x.watched||0)>=(+x.total||+x.knownTotal||0)).length,Array.isArray(ach)?ach.filter(Boolean).length:0];['accountAnimeCount','accountEpisodeCount','accountFavoriteCount','accountCompletedCount','accountAchievementCount'].forEach((id,i)=>$(id)&&($(id).textContent=new Intl.NumberFormat('es-ES').format(vals[i])))}
function decorate(){setTopProfile();stats();const n=$('profileName'),e=$('profileEmail'),d=$('profileEmailDisplay'),v=$('profileEmailVerify'),st=$('profileAccountStatus');if(n)n.value=profile?.name||'Usuario';if(e){e.value=user?.email||profile?.email||'';e.readOnly=!!user}if(d)d.textContent=user?.email||profile?.email||'Modo local';if(v)v.textContent=user?(user.email_confirmed_at?'✓ Correo verificado':'⚠ Correo pendiente de verificación'):'';if(st){st.textContent=user?'● Cuenta conectada':'○ Solo local';st.classList.toggle('online',!!user)}if($('accountCloudDot'))$('accountCloudDot').classList.toggle('online',!!user);if($('accountCloudTitle'))$('accountCloudTitle').textContent=user?'Sincronización activa':'Modo local';if($('accountCloudInfo'))$('accountCloudInfo').textContent=user?'Biblioteca y perfil guardados en tu cuenta.':'Tus datos están guardados en este navegador.';if($('cloudProfileLogout'))$('cloudProfileLogout').style.display=user?'inline-flex':'none';if($('cloudSyncFromProfile'))$('cloudSyncFromProfile').style.display=user?'inline-flex':'none';if($('accountPasswordReset'))$('accountPasswordReset').style.display=user?'inline-flex':'none';if($('profileSince'))$('profileSince').textContent=profile?.createdAt?`Perfil creado el ${new Date(profile.createdAt).toLocaleDateString('es-ES')}`:''}
function buildModal(){if(modalBuilt)return;const m=$('settingsModal'),c=m?.querySelector('.settingsCard,.accountModalCard,.modalCard');if(!m||!c)return;modalBuilt=true;c.classList.add('accountModalCard');c.innerHTML=`<button class="close" id="settingsClose" aria-label="Cerrar">×</button><div class="accountHero"><div class="accountAvatarBig" id="profileAvatar">AT</div><div class="accountIdentity"><h2>Mi cuenta</h2><span class="accountEmailLine" id="profileEmailDisplay">Modo local</span><span class="accountStatus" id="profileAccountStatus">○ Solo local</span></div><div class="accountActionsTop"><button class="btn" id="cloudSyncFromProfile">☁️ Sincronizar</button><button class="btn ghost" id="cloudProfileLogout">Cerrar sesión</button></div></div><div class="accountStats"><div class="accountStat"><strong id="accountAnimeCount">0</strong><span>Animes</span></div><div class="accountStat"><strong id="accountEpisodeCount">0</strong><span>Episodios</span></div><div class="accountStat"><strong id="accountFavoriteCount">0</strong><span>Favoritos</span></div><div class="accountStat"><strong id="accountCompletedCount">0</strong><span>Completados</span></div><div class="accountStat"><strong id="accountAchievementCount">0</strong><span>Logros</span></div></div><section class="accountSection"><h3>👤 Perfil</h3><div class="accountGrid"><div class="accountField"><label for="profileName">Nombre</label><input id="profileName" maxlength="32" placeholder="Tu nombre"></div><div class="accountField"><label for="profileEmail">Correo electrónico</label><input id="profileEmail" type="email" maxlength="120" placeholder="tu@correo.com"><div class="accountVerify" id="profileEmailVerify"></div></div></div><div class="accountAvatarTools"><button class="btn ghost" id="profilePhotoBtn">🖼️ Cambiar foto</button><button class="btn ghost" id="profilePhotoRemove">Quitar foto</button><input id="profilePhotoInput" type="file" accept="image/*" hidden><span class="profileHint">Máximo 2 MB · se guarda localmente y en la nube.</span></div><div class="dataActions"><button class="btn" id="profileSave">Guardar cambios</button></div></section><section class="accountSection"><h3>☁️ Nube</h3><div class="accountCloudBox"><div class="accountCloudBadge"><div class="accountCloudDot" id="accountCloudDot"></div><div class="accountCloudText"><strong id="accountCloudTitle">Modo local</strong><span id="accountCloudInfo">Tus datos están guardados en este navegador.</span></div></div><div class="dataActions" style="margin-top:0"><button class="btn" id="accountPasswordReset">🔑 Cambiar contraseña</button></div></div></section><section class="accountSection"><h3>🛡️ Protección</h3><label class="settingToggle"><span>Confirmar eliminaciones<small>Evita borrar un anime por accidente.</small></span><input id="settingConfirmDelete" type="checkbox"></label><label class="settingToggle"><span>Copias automáticas<small>Guarda instantáneas locales periódicas.</small></span><input id="settingAutoBackup" type="checkbox"></label><label class="settingToggle"><span>Avisos de logros<small>Muestra la ventana emergente al desbloquearlos.</small></span><input id="settingAchievementPopups" type="checkbox"></label></section><section class="accountSection"><h3>💾 Tus datos</h3><p>Exporta tu biblioteca completa, importa una copia o crea un respaldo local.</p><div class="dataActions"><button class="btn" id="settingsExport">Exportar copia</button><button class="btn" id="settingsImport">Importar</button><button class="btn" id="backupNow">Crear copia local</button><button class="btn dangerAction" id="resetAll">Borrar datos locales</button></div><div class="storageLine"><span id="storageUsed">—</span><span>almacenamiento local</span></div><div class="storageMeter"><span id="storageBar"></span></div></section><div class="accountFooter"><span id="profileSince" class="profileHint"></span><button class="btn ghost" id="accountCloseBottom">Cerrar</button></div>`;
$('settingsClose').onclick=()=>m.classList.add('hidden');$('accountCloseBottom').onclick=()=>m.classList.add('hidden');$('profileSave').onclick=saveProfile;$('profilePhotoBtn').onclick=()=>$('profilePhotoInput').click();$('profilePhotoInput').onchange=photo;$('profilePhotoRemove').onclick=removePhoto;$('cloudSyncFromProfile').onclick=syncNow;$('cloudProfileLogout').onclick=logout;$('accountPasswordReset').onclick=resetPassword;['settingConfirmDelete','settingAutoBackup','settingAchievementPopups'].forEach(id=>$(id)?.addEventListener('change',()=>{settings={...settings,confirmDelete:$('settingConfirmDelete').checked,autoBackup:$('settingAutoBackup').checked,achievementPopups:$('settingAchievementPopups').checked};write(KEY_SETTINGS,settings);notify('Ajustes guardados')}));$('settingsExport').onclick=()=>typeof exportComplete==='function'&&exportComplete();$('settingsImport').onclick=()=>$('importFile')?.click();$('backupNow').onclick=()=>{if(typeof createBackup==='function')createBackup('Manual',true);notify('Copia local creada')};$('resetAll').onclick=resetLocal;}
async function loadProfile(){if(!user)return;const {data,error}=await sb.from('profiles').select('username,avatar_data,created_at').eq('id',user.id).maybeSingle();if(error){console.warn(error);return}if(data){profile={...profile,name:data.username||profile.name||'Usuario',email:user.email||profile.email||'',avatar:data.avatar_data||profile.avatar||'',createdAt:profile.createdAt||Date.parse(data.created_at||'')||Date.now()};write(KEY_PROFILE,profile)}else await pushProfile()}
async function pushProfile(){if(!user)return;const {error}=await sb.from('profiles').upsert({id:user.id,username:(profile.name||'Usuario').slice(0,32),avatar_data:profile.avatar||null,updated_at:new Date().toISOString()},{onConflict:'id'});if(error)throw error}
function snapshot(){const o={};KEYS.forEach(k=>{const v=localStorage.getItem(k);if(v!==null)o[k]=v});return o}
async function fetchCloud(){const {data,error}=await sb.from('tracker_state').select('state,updated_at').eq('user_id',user.id).maybeSingle();if(error)throw error;return data||null}
async function pushState(){const {error}=await sb.from('tracker_state').upsert({user_id:user.id,state:snapshot(),updated_at:new Date().toISOString()},{onConflict:'user_id'});if(error)throw error;localStorage.setItem(KEY_LAST,String(Date.now()))}
async function pullState(remote){if(!remote?.state)return false;Object.entries(remote.state).forEach(([k,v])=>localStorage.setItem(k,v));return true}
async function reconcile(reason){if(!user)return;const remote=await fetchCloud(),local=library(),cloud=remoteLibrary(remote?.state),rt=remote?Date.parse(remote.updated_at||'')||0:0,lt=localStamp();if(!remote){if(local.length)await pushState();return}if(!local.length&&cloud.length){await pullState(remote);notify(reason==='startup'?'☁️ Biblioteca recuperada':'☁️ Biblioteca recuperada de la nube');setTimeout(()=>location.reload(),250);return}if(local.length&&!cloud.length){await pushState();return}if(!local.length&&!cloud.length)return;if(rt>lt){await pullState(remote);notify('☁️ Se recuperó la versión más reciente');setTimeout(()=>location.reload(),250);return}await pushState()}
async function syncNow(){if(!user){notify('Inicia sesión para sincronizar');return}try{await reconcile('manual');await loadProfile();decorate();if(library().length)notify('☁️ Sincronización completada')}catch(e){console.warn(e);notify('No se pudo sincronizar; tus datos locales siguen intactos')}}
async function saveProfile(){profile={...profile,name=(($('profileName')?.value||'').trim().slice(0,32)||'Usuario'),email:user?.email||profile.email||'',createdAt:Number(profile.createdAt)||Date.now()};write(KEY_PROFILE,profile);try{if(user)await pushProfile();decorate();notify(user?'Perfil guardado y sincronizado ☁️':'Perfil guardado')}catch(e){console.warn(e);decorate();notify('Perfil guardado localmente')}}
async function photo(e){const f=e.target.files?.[0];if(!f)return;if(!f.type.startsWith('image/'))return notify('Selecciona una imagen válida');if(f.size>2*1024*1024)return notify('Máximo 2 MB');const r=new FileReader();r.onload=async()=>{profile={...profile,avatar:String(r.result||'')};write(KEY_PROFILE,profile);decorate();try{if(user)await pushProfile();notify(user?'Foto sincronizada ☁️':'Foto actualizada')}catch{notify('Foto guardada localmente')}};r.readAsDataURL(f)}
async function removePhoto(){profile={...profile,avatar:''};write(KEY_PROFILE,profile);decorate();try{if(user)await pushProfile()}catch{}notify('Foto eliminada')}
async function resetPassword(){if(!user?.email)return;const {error}=await sb.auth.resetPasswordForEmail(user.email,{redirectTo:location.origin+location.pathname});notify(error?'No se pudo enviar el correo':'📧 Revisa tu correo para cambiar la contraseña')}
async function logout(){if(!confirm('¿Cerrar sesión? Tus datos locales permanecerán guardados.'))return;await sb.auth.signOut();user=null;decorate();$('settingsModal')?.classList.add('hidden');notify('Sesión cerrada')}
function resetLocal(){if(!confirm('¿Borrar TODOS los datos locales?'))return;if(!confirm('Se eliminarán biblioteca, perfil, logros, copias y ajustes. ¿Continuar?'))return;if(typeof exportComplete==='function')exportComplete();[KEY_STATE,KEY_PROFILE,KEY_SETTINGS,'anime_tracker_activity',KEY_ACH,'anime_tracker_theme','anime_tracker_custom_theme','anime_tracker_trash_v1','anime_tracker_backups_v1',KEY_LAST].forEach(k=>localStorage.removeItem(k));location.reload()}
function storage(){let b=0;for(let i=0;i<localStorage.length;i++){const k=localStorage.key(i)||'',v=localStorage.getItem(k)||'';b+=(k.length+v.length)*2}if($('storageUsed'))$('storageUsed').textContent=b<1048576?(b/1024).toFixed(1)+' KB':(b/1048576).toFixed(2)+' MB';if($('storageBar'))$('storageBar').style.width=Math.min(100,b/(5*1024*1024)*100)+'%';if($('settingConfirmDelete'))$('settingConfirmDelete').checked=!!settings?.confirmDelete;if($('settingAutoBackup'))$('settingAutoBackup').checked=!!settings?.autoBackup;if($('settingAchievementPopups'))$('settingAchievementPopups').checked=!!settings?.achievementPopups}
async function openProfile(){buildModal();storage();decorate();$('settingsModal')?.classList.remove('hidden');if(user)try{await loadProfile();decorate()}catch{}}
async function init(){user=(await sb.auth.getUser()).data?.user||null;setTopProfile();buildModal();decorate();if(user){await loadProfile();decorate();await reconcile('startup')}}
window.addEventListener('animetracker:saved',()=>{clearTimeout(syncTimer);syncTimer=setTimeout(()=>{if(user)reconcile('autosave').catch(console.warn)},900)});
window.addEventListener('load',()=>{document.querySelectorAll('.cloud-account-btn').forEach(x=>x.style.display='none');const b=$('profileTopBtn');if(b)b.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();openProfile()},{capture:true});init().catch(console.warn)});
sb.auth.onAuthStateChange(async(_e,s)=>{user=s?.user||null;setTopProfile();if(user){try{await loadProfile();await reconcile('login')}catch(e){console.warn(e)}}decorate()});
window.AnimeTrackerCloud={sync:syncNow,openProfile,refresh:init};
})();